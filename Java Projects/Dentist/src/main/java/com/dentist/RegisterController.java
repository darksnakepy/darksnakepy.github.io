package com.dentist;

import com.dentist.sqllite.SQL_methods;
import javafx.scene.control.Alert;

public class RegisterController {
    static SQL_methods sql = new SQL_methods();
    /*
    protected boolean getData(){
        if(/* handle not empty forms){
            Alert alert = new Alert(Alert.AlertType.ERROR);
            alert.setTitle("Error");
            alert.setContentText("Make sure all text areas are filled.");
            alert.showAndWait();
        }else{
            sql.registerPatient();
            return true;
        }

    }

    protected boolean blank(){
        // make all the forms blank again
    }


    protected void exitRegister(){
        Stage stage;
        // handle the closing event with alert
    }
     */
}
